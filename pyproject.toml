[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "mitm_mock"
version = "0.1.0"
description = "Simple MITM proxy mock"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "mitmproxy",
    "pyyaml",
    "watchdog",
]
